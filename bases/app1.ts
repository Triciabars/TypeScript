(() => {
    const a : number = 10;
    console.log(a);
})() //Función anónima autoinvocada

//tsc --watch (transpira todos los archivos en el proyecto)
//tsc --init (archivo de configuración)
//tsc app.ts (solo transpila este archivo)
